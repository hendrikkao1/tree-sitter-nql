==================
enum
==================

devices

| where
  field == red

| where
  state in [enum, ENUM, Enum, en_uM]

---

(source_file
      (query
        (select_clause
          (table))
        (clause
          (pipe)
          (where_clause
            (comparison
              (field
                (field_name))
              (equals)
              (enum))))
        (clause
          (pipe)
          (where_clause
            (comparison
              (field
                (field_name))
              (in_array)
              (enum)
              (enum)
              (enum)
              (enum))))))